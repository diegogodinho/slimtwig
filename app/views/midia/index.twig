{% extends 'default.twig' %}
{% block title %}
    Midias
{% endblock %}
{% block breadcrumbs %}
    <li>
        <i class="fa fa-angle-right"></i>
        <a href="{{path_for('midia')}}">Midias</a>
    </li>
{% endblock %}
{% block content %}    
    <div class="row custom-spacing-top">
        <div class="col-lg-1">
            <a href="{{ path_for('midia.createview') }}" class="btn btn-primary">Nova Midia</a>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12">
            <div class="box box-color box-bordered">
                <div class="box-title">
                    <h3>
                        <i class="fa fa-table"></i>
                        Midias
                    </h3>
                </div>
                <div class="box-content nopadding">
                    <input type="hidden" id="Titulo" value="Bairro"/>

                    <table class="table table-hover table-nomargin table-bordered order-column datatable" data-path="{{path_for('midia')}}" data-path-actdeact="{{ path_for('midia.actdeact',{'id' : 0 }) }}" data-path-edit="{{ path_for('midia.editview',{'id' : 0 }) }}" id="tagtable">
                        <thead>
                            <tr>
                                <th>Nome</th>
                                <th>Status</th>
                                <th></th>
                            </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block script %}
    <script src="{{base_url()}}/app/template/interno/js/plugins/datatables/jquery.dataTables.js"></script>
    <script src="{{base_url()}}/app/scriptsjs/midia/index.js"></script>
    {% if errors %}
        {# <script type="text/javascript">
            $(function () {
                //$('#modal-validation-errors').modal('show');
            });
        </script> #}
    {% endif %}

{% endblock %}